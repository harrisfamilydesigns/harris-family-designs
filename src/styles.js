const card = {
  standard: {
    width: '100%',
    width: 600,
    margin: 'auto',
    marginTop: 20,
    marginBottom: 20,
  },
};

const row = {
  flexRowCenterCenter: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
  }
}

export {
  card,
  row,
}

